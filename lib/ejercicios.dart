import 'package:flutter/material.dart';

class EjerciciosPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Menú'),
      ),
      body: Center(
        child: Text('Contenido del menú Ejercicios'),
      ),
    );
  }
}
